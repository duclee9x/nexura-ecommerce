import type { sendUnaryData, ServerUnaryCall } from "@grpc/grpc-js";
 
export * from "./health-service"
// export * from "./load-proto"
// Type for asyncHandler: wraps a gRPC handler that returns a Promise
