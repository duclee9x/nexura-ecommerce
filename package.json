{
  "name": "nexura",
  "module": "index.ts",
  "workspaces": [
    "src/*",
    "packages/*"
  ],
  "scripts": {
    "proto": "docker compose -f docker-compose-dev.yml up protoc -d && mv packages/grpc_gateway/protos/generated/nexura_pb2* src/email-service/protos"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
